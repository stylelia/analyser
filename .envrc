# Configure docker
export REDIS_HOST="127.0.0.1"
export REDIS_PORT="6379"
export REDIS_PASSWORD="MySecurePassword"
# Start redis in docker
docker-compose up -d
export ORGANISATION=stylelia
export NAME=snort